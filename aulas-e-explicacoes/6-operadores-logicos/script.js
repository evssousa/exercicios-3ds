/** OPERADORES LÓGICOS
 * 
 * Tabela Verdade
 * 
 * E (AND). Só é verdadeiro se ambas as partes forem verdadeiras. Operador &&.
 * true && true === true
 * true && false === false
 * false && false === false
 * false && true === false
 * 
 * OU (OR). Só é falso se ambas as partes forem falsa. Operador ||.
 * true || true === true
 * true || false === true
 * false || false === false
 * false || true === true
 * 
 * NÃO (NOT). Inverte o resultado (de verdadeiro para falso e vice-versa). Operador !.
 * !true === false
 * !false === true
 */